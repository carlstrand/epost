user = {{ mailserver_db_username }}
password = {{ mailserver_db_password }}
hosts = {{ mailserver_db_host }}
dbname = {{ mailserver_db_database }}
query = SELECT name FROM virtual_domains WHERE name='%s'
